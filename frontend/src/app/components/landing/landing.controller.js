export default class LandingController {

    constructor($log, $timeout) {
        'ngInject';
        this.$log = $log;
        this.$timeout = $timeout;

        //this.msg = "for martin";
    }
}

